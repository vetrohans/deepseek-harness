// Pre-built plugin package: the node/client halves ship as ready-to-serve
// bundles (lib/index.js, lib/client.js); skip the workspace tsdown pass —
// there is no source compile step for this package.
export default { entry: '' }
